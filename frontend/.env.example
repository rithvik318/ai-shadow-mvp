# Where the frontend sends API calls. Leave unset to use the dev proxy in
# vite.config.ts, which forwards /api to the backend without needing CORS.
# VITE_API_BASE_URL=/api

# Backend origin the dev proxy forwards to.
# VITE_BACKEND_ORIGIN=http://localhost:8000
